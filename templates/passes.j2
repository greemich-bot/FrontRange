{% extends "main.j2" %}
{% block content %}

<h1>Passes</h1>

{# READ table #}
<table>
    <thead>
        {# For the table header row, we print the DB attribute names #}
        <tr>
            {% for key in passes[0].keys() %}
            <th>{{ key }}</th>
            {% endfor %}
            <th>Delete Passes</th>
           
        </tr>
    </thead>

    <tbody>
        {# For each row, print the PassID, Type, PurchaseDate, ExpirationDate, Skiers_SkierID #}
        {% for pass in passes %}
        <tr>
            <td>{{ pass.PassID }}</td>
            <td>{{ pass.Type}}</td>
            <td>{{ pass.PurchaseDate }}</td>
            <td>{{ pass.ExpirationDate }}</td>
            <td>{{ pass.Skiers_SkierID }}</td>

             {# DELETE form #}
            <td>
                <form class="cuForm" id="delete_pass_form_{{ pass.PassID }}" method="POST" action="/passes/delete">
                    <input type="hidden" name="delete_pass_id" value="{{ pass.PassID }}">
                    <input type="submit" value="Delete">
                </form>
            </td>

            </form>
        </tr>
        {% endfor %}
    </tbody>
</table>

--- create passes ----
<h2>Create Passes</h2>


<form class="cuForm" id="create_passes_form" method="POST" action="/passes/create">

<label>Type:</label>
<select name="create_pass_type" required>
    <option disabled selected>Select pass type</option>
    <option value="Full Season">Full Season</option>
    <option value="Day Pass">Day Pass</option>
    <option value="3-Day Pass">3-Day Pass</option>
</select>

<label>Purchase Date:</label>
<input type="datetime-local"
       name="create_purchase_date"
       required>

<label>Expiration Date:</label>
<input type="datetime-local"
       name="create_expiration_date"
       required>

<label>Skier:</label>
<select name="create_skier_skierId" required>

    <option value="" disabled selected>
        Select Skier
    </option>

    {% for skier in skiers %}
        <option value="{{ skier.SkierID }}">
            {{ skier.Name }}
        </option>
    {% endfor %}

</select>

<input type="submit">

</form>

{% endblock %}